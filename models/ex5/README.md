# EX5 model artifacts

`run_stable_trait_pitch_probe.py` writes the final 2024-fold CatBoost probe for each EX5 ablation here.

These are diagnostic research artifacts only. They are not submission models and are not blended with SAFE982.

Expected local files (ignored by git):

```text
stable_trait_pitch_probe_prior_success_only.cbm
stable_trait_pitch_probe_stable4_raw.cbm
stable_trait_pitch_probe_stable4_reliable.cbm
stable_trait_pitch_probe_stable4_reliable_plus_middle.cbm
```
