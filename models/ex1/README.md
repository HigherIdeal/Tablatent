# EX1 generated models

This directory is reserved for models produced by `scripts/ex1/run_reverse_expert.py`.

Expected local artifacts:

```text
models/ex1/reverse_structural.cbm
models/ex1/reverse_full.cbm
```

Generated `.cbm` files are experiment artifacts and should not be promoted into the SAFE baseline unless EX1 passes the follow-up validation criteria.
