# EX3 generated models

This directory is reserved for local artifacts produced by `scripts/ex3/run_four_state_backward.py`.

Expected files:

```text
models/ex3/four_state_backward_state_only.cbm
models/ex3/four_state_backward_context_state.cbm
```

These are experimental backward reconstruction models. They are not part of SAFE982 and must not be promoted without follow-up validation.
